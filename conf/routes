# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
#GET     /                                  controllers.Application.toPrefix()
GET     /wm2014                            controllers.Application.index

# Map js routes
GET        /routes                     controllers.Application.routes
GET        /jsroutes.js                controllers.Application.jsRoutes(varName ?= "jsRoutes")

# Map static resources from the /public folder to the /assets URL path
GET     /wm2014/*file                  controllers.GulpAssets.at(file)
