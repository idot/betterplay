# This is the main configuration file for the application.
# ~~~~~

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
#
# This must be changed for production, but we recommend not changing it in this file.
#
# See http://www.playframework.com/documentation/latest/ApplicationSecret for more details.
play.crypto.secret="QF0[p<xNA:Lwp3KVRAsoPXlLMA<3BAC5af1cLT1XvS[N/lsDw2CobXbwVuKi`dFo"

# The application languages
# ~~~~~
play.i18n.langs = [ "en" ]

#test or euro2016
#unit tests need test! => maybe fix it in the testing code with override
#betterplay.insertdata = "euro2016"  
betterplay.insertdata = "test"
betterplay.debug = true

play.modules.enabled  += "models.BetterModule"

betterplay.excelSecret = "for testing"
betterplay.superpassword = "for testing"

# Global object class
# ~~~~~
# Define the Global object class for this application.
# Default to Global in the root package.
# application.global=Global

# Router
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point.
# Furthermore, it's assumed your route file is named properly.
# So for an application router like `my.application.Router`,
# you may need to define a router file `conf/my.application.routes`.
# Default to Routes in the root package (and conf/routes)
# application.router=my.application.Routes

# Database configuration
# ~~~~~
# You can declare as many datasources as you want.
# By convention, the default datasource is named `default`
#

slick.dbs.default.driver="slick.driver.H2Driver$"
slick.dbs.default.db.driver="org.h2.Driver"
slick.dbs.default.db.url="jdbc:h2:mem:play"
slick.dbs.default.db.user=sa
slick.dbs.default.db.password=""

#slick.dbs.default.db.driver="slick.driver.PostgresDriver$"
#slick.dbs.default.db.password=better
#slick.dbs.default.db.user=better
#slick.dbs.default.db.driver=org.postgresql.Driver
#slick.dbs.default.db.url="postgres://better:better@jenkins/better"
#slick.dbs.default.db.url="postgres://better:better@localhost/better"


#gulp.devDirs=["ui/.tmp/serve", "ui/src", "ui"]
#gulp.devDirs=["ui/app"]

play.mailer {
  host = "localhost"
  port = 25
  ssl = "no"
  tls = "no"
  user = "nouser"
  password = "nopassword"
  debug = "yes"
  timeout = 60000
  connectiontimeout = 60000
  mock = "yes"
}


# Slick Evolutions
# # ~~~~~
# slick.default="models.*"

# Evolutions
# ~~~~~
# You can disable evolutions if needed
# evolutionplugin=disabled


play {
  akka {
    actor {
      slick-context = {
        fork-join-executor {
          parallelism-min = 200
          parallelism-max = 300
        }
      }
    }
  }
}
